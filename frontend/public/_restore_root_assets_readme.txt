These common branding/static files were moved into a deep images folder earlier.
Some deployment setups or meta tags expect them at the public root.
If you need root references (e.g. /favicon.svg, /logo.svg), copy them back.
Automated restore handled via commit.